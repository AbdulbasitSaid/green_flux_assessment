import 'package:flutter/material.dart';
import 'package:green_flux_assessment/features/charge_locations/application/widgets/search_bar.dart'
    as searchBar;

class ChargeLocationDetails extends StatelessWidget {
  const ChargeLocationDetails({super.key, required this.locationId});
  final String locationId;
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
